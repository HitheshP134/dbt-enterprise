version: 2

sources:
  - name: raw
    description: Raw seed data simulating source system extracts
    schema: stage
    tables:
      - name: raw_customers
        description: Customer account records from CRM

      - name: raw_products
        description: Product catalog from ERP

      - name: raw_employees
        description: Employee records from HRIS

      - name: raw_orders
        description: Sales order transactions from CRM/ERP

